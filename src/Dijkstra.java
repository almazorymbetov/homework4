import java.util.*;
public class Dijkstra implements Search<V>{
    public List<Vertex<V>> search(Vertex<V> first,Vertex<V> last){
        Map<Vertex<V>,Double> length=new HashMap<>();
        Map<Vertex<V>,Vertex<V>> prev=new HashMap<>();
        PriorityQueue<Vertex<V>> queue=new PriorityQueue<>(Com)
    }
}
